@inherits LayoutComponentBase

<div class="page">
    <RadzenLayout Style="width: 100vw">
        <RadzenHeader>
            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0">
                <RadzenSidebarToggle Click="@ToggleSideBar" />
                <RadzenLabel Text="CategorEyes" />
            </RadzenStack>
        </RadzenHeader>
        <RadzenSidebar @bind-Expanded="@expandedSideBar">
            <RadzenPanelMenu>
                <RadzenPanelMenuItem Text="Analysis" Icon="psychology" Path="/" />
                <RadzenPanelMenuItem Text="Historical" Icon="insights" Path="historical" />
            </RadzenPanelMenu>
        </RadzenSidebar>
        <RadzenBody>
            <div class="rz-p-12">
                @Body
            </div>
        </RadzenBody>
        <RadzenFooter Style="">
            Blake T. Attaway - 2024
        </RadzenFooter>
    </RadzenLayout>
    <RadzenComponents />
</div>

@code {
    bool expandedSideBar = true;

    void ToggleSideBar()
    {
        expandedSideBar = !expandedSideBar;
    }
}